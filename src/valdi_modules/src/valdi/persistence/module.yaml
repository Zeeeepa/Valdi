output_target: release

bazel_build_file_generation_disabled: true

dependencies:
  - coreutils
  - valdi_core
  - valdi_standalone

allowed_debug_dependencies:
  - valdi_standalone

exclude_patterns:
  - .*\/web\/.*
exclude_globs:
  - "**/web/**"
