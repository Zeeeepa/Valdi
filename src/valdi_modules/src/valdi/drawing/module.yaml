output_target: release

bazel_build_file_generation_disabled: true

dependencies:
  - valdi_core

allowed_debug_dependencies:
  - valdi_test
  - jasmine

exclude_patterns:
  - .*\/web\/.*
exclude_globs:
  - "**/web/**"
